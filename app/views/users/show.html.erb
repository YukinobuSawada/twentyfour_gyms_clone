<h2>マイページ</h2>
<table class="table">
	<tbody>
		<tr>
			<td>名前</td>
			<td><%= @user.name %>
		</tr>
		<tr>
			<td>メールアドレス</td>
			<td><%= @user.email %></td>
		</tr>
		<tr>
			<td>
		<% if @user.id == current_user.id %>
<p><%= link_to edit_user_path(@user) do %>
	<button class= "btn btn-outline-primary">
	プロフィール編集</button>
<% end %>
</p>
<% end %>
</td>
<td></td>
</tr>

	</tbody>